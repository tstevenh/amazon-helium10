# Local development only (npm run dev outside Docker).
# NEXT_PUBLIC_API_URL uses the /backend proxy so all API calls are same-origin.
# API_URL tells the Next.js rewrite proxy where the real backend is.
NEXT_PUBLIC_API_URL=/backend
API_URL=http://localhost:8000
