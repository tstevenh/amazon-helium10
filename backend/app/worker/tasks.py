"""Background Amazon sync tasks.

Placeholder — the sync_account task is added in Plan 2 Task 3. This module
exists now because celery_app.py lists it in `include`, and Celery imports
every listed module at worker startup.
"""
